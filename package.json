{
  "name": "visual-code-time-machine",
  "displayName": "Visual Code Time Machine",
  "description": "Local data-capture layer for visualizing how code evolves over time",
  "version": "0.1.1",
  "publisher": "MuhammadEhsaan",
  "icon": "images/icon.png",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mehsaandev/code-time-machine"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:visualCodeTimeMachine.startSession"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "visualCodeTimeMachine.startSession",
        "title": "Code Time Machine: Start Session"
      },
      {
        "command": "visualCodeTimeMachine.stopSession",
        "title": "Code Time Machine: Stop Session"
      },
      {
        "command": "visualCodeTimeMachine.rebuildFileAtTime",
        "title": "Code Time Machine: Rebuild File At Time"
      },
      {
        "command": "visualCodeTimeMachine.pauseRecording",
        "title": "Code Time Machine: Pause Recording"
      },
      {
        "command": "visualCodeTimeMachine.resumeRecording",
        "title": "Code Time Machine: Resume Recording"
      }
    ],
    "configuration": {
      "title": "Code Time Machine",
      "properties": {
        "visualCodeTimeMachine.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable code change recording (opt-in required)"
        },
        "visualCodeTimeMachine.batchIntervalMs": {
          "type": "number",
          "default": 3000,
          "description": "Batch interval for capturing diffs (in milliseconds)"
        },
        "visualCodeTimeMachine.idleTimeoutMinutes": {
          "type": "number",
          "default": 15,
          "description": "Minutes of inactivity before session ends"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/uuid": "^10.0.0",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "eslint": "^8.54.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "diff-match-patch": "^1.0.5",
    "sql.js": "^1.13.0",
    "uuid": "^9.0.0"
  }
}
